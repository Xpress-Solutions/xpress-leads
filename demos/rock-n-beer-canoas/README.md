# Rock'n Beer Pub — demo

One-page da casa na Av. Sezefredo Azambuja Vieira, 266, Canoas.

Identidade em `brand.json`. Cores CSS (`--background`, `--brand`, `--accent`…) saem dali.

```bash
npm install
npm run dev
npm run build
```
