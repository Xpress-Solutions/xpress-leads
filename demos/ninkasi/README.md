# Ninkasi Bar — demo Xpress

One-page da casa na Rua João Alfredo, 557 (Cidade Baixa, Porto Alegre).

Identidade em `brand.json` (schema `BrandIdentity` em `lib/brand.ts`). Cores da fachada de Ana Scarcelli + vermelho-tijolo `#AC2A17` da Sapatista.

```bash
npm install
npm run dev
npm run build
```
