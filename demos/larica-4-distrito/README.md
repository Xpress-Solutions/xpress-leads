# Larica 4º Distrito — demo

One-page da esquina na Av. Santos Dumont, 710 (Floresta / 4º Distrito, Porto Alegre).

Identidade em `brand.json` (placa corten, telha ondulada, neon verde). Não é template genérico.

```bash
npm install
npm run dev
npm run build
```

Sem deploy Vercel nesta branch.
