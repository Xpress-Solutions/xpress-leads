# Eucaliptos Sports Bar — demo

One-page do sports bar do Menino Deus (Rua Barão do Cerro Largo, 300).

Identidade em `brand.json` (schema `lib/brand.ts`): carvão + âmbar de chope, slab varsity, hero da fachada real.

```bash
npm install
npm run dev
npm run build
```
